function Footer() {
  return (
    <footer className="footer">
      <div className="container footer-inner">
        <span>React UI + .NET API + MySQL</span>
        <span className="muted">API routes are available under `/api` and `/uploads`.</span>
      </div>
    </footer>
  )
}

export default Footer
